<UserControl x:Class="sh.Creator.Views.UC_Data"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:sh.Creator.Views"
             xmlns:vm="clr-namespace:sh.Creator.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="200">
    <Border BorderThickness="1" CornerRadius="5" Background="Gray" >
        <WrapPanel >
            <Label Content="{Binding Key}"  Foreground="White"></Label>
            <Border BorderThickness="1" CornerRadius="3" Background="Green">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCommon}" Value="True">
                                <Setter Property="Background" Value="Red"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Label Content="{Binding Value}" Foreground="White"></Label>

            </Border>
            <Label Content="{Binding IsCommon}" Foreground="White"></Label>
        </WrapPanel>
    </Border>

</UserControl>
